# ===========================================
# VoaLive (ReservaSegura) - Environment Variables
# ===========================================

# NODE ENVIRONMENT
NODE_ENV=development
PORT=3012

# DATABASE (PostgreSQL)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://reservasegura_user:your_password_here@localhost:5432/reservasegura_dev

# REDIS CACHE
# Format: redis://:[password]@[host]:[port]
REDIS_URL=redis://:your_redis_password@localhost:6379

# FIRECRAWL API (Web Scraping)
# Get your API key at: https://firecrawl.dev/dashboard
FIRECRAWL_API_KEY=fc-your_firecrawl_api_key_here

# SCRAPER CONFIGURATION
AUTO_START_SCRAPER=false
LOG_LEVEL=debug

# JWT AUTHENTICATION
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_minimum_32_characters_long

# NEXTAUTH.JS CONFIGURATION
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_minimum_32_characters_long
NEXTAUTH_URL=http://localhost:3011

# API URLS
NEXT_PUBLIC_API_URL=http://localhost:3012

# ===========================================
# PRODUCTION ONLY (não usar em desenvolvimento)
# ===========================================

# SSL/HTTPS Configuration
# NEXTAUTH_URL=https://reservasegura.com
# NEXT_PUBLIC_API_URL=https://api.reservasegura.com

# Production Database
# DATABASE_URL=postgresql://user:password@production-host:5432/reservasegura_prod

# Production Redis
# REDIS_URL=redis://:password@production-host:6379

# Email Configuration (if needed)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# SMTP_FROM=noreply@reservasegura.com

# Storage (if using S3 or similar)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1
# S3_BUCKET=reservasegura-uploads

# ===========================================
# NOTES
# ===========================================
# 1. Copy this file to .env and fill with your values
# 2. Never commit .env file to git
# 3. Use different secrets for dev and production
# 4. Rotate secrets regularly (every 90 days)
# 5. For production, configure secrets in GitHub Actions
