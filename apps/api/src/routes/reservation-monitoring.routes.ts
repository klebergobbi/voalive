/**
 * RESERVATION MONITORING ROUTES
 * Rotas para o sistema de monitoramento de reservas
 *
 * Base path: /api/v2/monitoring
 */

import reservationMonitoringController from '../controllers/reservation-monitoring.controller';

export default reservationMonitoringController;
